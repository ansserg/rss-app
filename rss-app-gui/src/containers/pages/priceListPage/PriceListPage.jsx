import React, { useState } from 'react';
// import { useSelector } from "react-redux";
// import PriceList from "../../components/Page/priceList";
// import PriceCheckBar from "../../components/Page/priceCheckBar";
// import PriceListControl from "../../components/priceListControl";

const PriceListPage = () => {
    // const [isTableOpen, setTableOpen] = useState(true);
    // const taskNumber=useSelector((state)=>state.priceListReducer.task);
    // const dataTbl=useSelector((state)=>state.priceListReducer.data);

    return (
        <>
        <h3>Страница ы стадии разработки</h3>
            {/* <PriceListControl />
            {
                isTableOpen &&
                <div style={{ paddingLeft: 10 }}>
                    <PriceCheckBar task={taskNumber} />
                    <PriceList data={dataTbl} />
                </div>
            } */}
        </>

    );
};
export default PriceListPage;

